# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Split App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
	# Add Parse pods and install
 	pod 'Parse'
 	pod 'AlamofireImage'

  # Pods for Split App

  target 'Split AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Split AppUITests' do
    # Pods for testing
  end

end
